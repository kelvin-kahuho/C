#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 on success, non-zero value on error
 */
int main(void)
{
	int i = 10;
	char name = "Meg";

	/* Function to print */
	printf("%c is %d old", name, i);
	return (0);
}
